{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Ports and Protocols Schema",
  "type": "array",
  "items": {
    "type": "object",
    "required": [
      "id",
      "service",
      "port",
      "transport",
      "description",
      "category",
      "securityNotes",
      "keywords"
    ],
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^port-[a-z0-9-]+$"
      },
      "service": {
        "type": "string",
        "minLength": 2
      },
      "port": {
        "type": ["string", "null"],
        "description": "Port number or range (e.g. '53', '67/68'), or null for non-transport IP protocols"
      },
      "protocolNumber": {
        "type": ["integer", "null"],
        "description": "IANA IPv4/IPv6 Protocol Number for transport='IP Protocol' (e.g. 1 for ICMP, 50 for ESP, 51 for AH)"
      },
      "transport": {
        "type": "string",
        "enum": ["TCP", "UDP", "TCP/UDP", "IP Protocol"]
      },
      "description": {
        "type": "string",
        "minLength": 10
      },
      "category": {
        "type": "string",
        "enum": [
          "Core Infrastructure",
          "Remote Access & Administration",
          "Web & Mail Services",
          "Directory & Identity",
          "Monitoring & Storage",
          "Network & Routing"
        ]
      },
      "securityNotes": {
        "type": "string",
        "minLength": 10
      },
      "keywords": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "additionalProperties": false
  }
}
